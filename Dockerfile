# Використовуємо легку версію Node.js (Alpine Linux)
FROM node:18-alpine

# Встановлюємо робочу директорію всередині контейнера
WORKDIR /app

# Спочатку копіюємо package.json, щоб встановити залежності
# Це робиться окремим кроком для кешування (щоб не перевстановлювати модулі при кожній зміні коду)
COPY package.json .

# Встановлюємо залежності (express і т.д.)
RUN npm install

# Копіюємо решту файлів проекту (ваш index.js)
COPY . .

# Вказуємо порт, на якому працює додаток (для документації, Render знайде його сам)
EXPOSE 3000

# Команда для запуску додатка
# ВАЖЛИВО: переконайтеся, що ваш файл з кодом називається index.js
CMD ["node", "index.js"]
